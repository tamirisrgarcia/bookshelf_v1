import { PipeTraducaoPipe } from './pipe-traducao.pipe';

describe('PipeTraducaoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTraducaoPipe();
    expect(pipe).toBeTruthy();
  });
});
